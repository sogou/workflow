language: cpp
os: windows
git:
  depth: 3
  quiet: true
branches:
  only:
  - windows

before_install:
- choco install -y openssl

script: 
- cmake -B build -S . && cmake --build build --config Release && cmake -B build_tutorial -S tutorial && cmake --build build_tutorial --config Release
